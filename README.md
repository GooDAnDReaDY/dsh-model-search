# @goodandready-private/dsh-model-search

Плагин для DeepSeek Harness WebUI: быстрый живой поиск и фильтрация моделей в выпадающем списке выбора модели.

## Возможности

- 🔍 **Мгновенная фильтрация**: поиск по названию модели, техническому идентификатору (ID) и имени провайдера на лету.
- 🔀 **Многословный поиск**: поддержка запросов из нескольких слов в любом порядке (например, `flash deepseek` найдет `DeepSeek-V4-Flash`).
- 🛡️ **Безопасная интеграция с React**: управление видимостью элементов без физической перестановки DOM-узлов ядра.
- 📐 **Полная совместимость с актуальным DSH**: корректная работа с двухуровневым селектором моделей (`root` $\leftrightarrow$ `model` $\leftrightarrow$ `effort`).
- 🌐 **Мультиязычность**: поддержка русского, английского и китайского интерфейсов.
- 🎨 **Нативная тема**: строгое использование официальных токенов темы DSH (светлая/тёмная).

## Установка

```bash
dsh plugin --profile web add @goodandready-private/dsh-model-search@0.1.0
```

## Разработка

```bash
# Проверка синтаксиса
pnpm run check

# Запуск модульных тестов
pnpm test
```

## Лицензия

MIT
